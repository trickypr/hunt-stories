{
  "name": "hunt-stories",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir build && mkdir build/pages && parcel build src/index.html --public-url ./ && cp dist/* build && cp pages/*.md build/pages/",
    "serve": "parcel src/index.html -p 8080"
  },
  "author": "trickypr",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "sass": "^1.23.0"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "base62.io": "^1.0.0",
    "marked": "^0.7.0"
  }
}
